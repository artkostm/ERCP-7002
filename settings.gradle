rootProject.name = 'ERCP-7002'

def subprojectMap = [
    ':configurator'                 : 'configurator',
    ':common'                       : 'common'
]

subprojectMap.each() { key, value ->
    include key
    project(key).name = value
}